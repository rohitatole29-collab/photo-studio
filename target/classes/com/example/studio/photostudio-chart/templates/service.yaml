apiVersion: v1
kind: Service
metadata:
  name: {{ include "photostudio-chart.fullname" . }}
  labels:
    {{- include "photostudio-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      targetPort: 8080
      name: http
  selector:
    {{- include "photostudio-chart.selectorLabels" . | nindent 4 }}
