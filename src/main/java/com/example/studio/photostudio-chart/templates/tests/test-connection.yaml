apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "photostudio-chart.fullname" . }}-test-connection"
  labels:
    {{- include "photostudio-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "photostudio-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
